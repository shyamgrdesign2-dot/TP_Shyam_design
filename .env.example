# Iconsax API key — server-only. Used by app/api/iconsax-icon to
# auto-match an icon for each custom module the doctor creates. Get a
# Pro key at https://app.iconsax.io and copy it into .env.local.
ICONSAX_API_KEY=
