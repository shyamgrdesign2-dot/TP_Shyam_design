export const metadata = {
  title: "TabRx Consultation — TatvaPractice",
  description:
    "Tablet point-and-write consultation — same RxPad shell as VoiceRx, with the prescription form replaced by a freehand drawing canvas.",
};

export default function Layout({ children }) {
  return children;
}
