export const metadata = {
  title: "TypeRx Consultation — TatvaPractice",
  description:
    "Type-only in-visit consultation — same RxPad shell as VoiceRx, with mic-driven features hidden.",
};

export default function Layout({ children }) {
  return children;
}
